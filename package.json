{
  "name": "expense-tracker-monorepo",
  "private": true,
  "dependencies": {
    "react-webcam": "^7.2.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "bestzip": "^2.2.1",
    "postcss": "^8.5.6",
    "rimraf": "^6.0.1",
    "shx": "^0.3.4",
    "tailwindcss": "^4.1.11"
  },
  "scripts": {
    "package:prep": "rimraf dist && shx mkdir -p dist",
  "package:source": "bestzip dist/expense-tracker-source.zip client/src client/public client/package.json client/README.md client/tailwind.config.js client/postcss.config.js client/cypress.config.js client/cypress server README.md LICENSE docs -r",
  "package:client": "node scripts/zip-client-build.js",
    "package:all": "npm run -s package:prep && npm run -s package:source && npm run -s package:client"
  }
}
